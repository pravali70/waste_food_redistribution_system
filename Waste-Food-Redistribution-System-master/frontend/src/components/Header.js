import React from 'react';
import '../styles.css';

const Header = () => {
  return (
    <header className="header">
      <a href="/" className="header-title">Welcome to Food Distribution System</a>
    </header>
  );
};

export default Header;
